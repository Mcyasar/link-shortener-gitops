apiVersion: v1
kind: Service
metadata:
  name: link-shortener-backend-service
spec:
  selector:
    app: link-shortener-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP