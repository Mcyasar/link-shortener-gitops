apiVersion: v1
kind: Service
metadata:
  name: "{{.Values.environment}}-docker-host"
  namespace: "linkshortener-{{.Values.environment}}"
spec:
  type: ExternalName
  externalName: host.docker.internal
